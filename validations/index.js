const calcTotal = (bookingType, basePrice, quantity, days, serviceFee) => {

  switch(bookingType) {
    case 'hourly':

      break;
    case 'daily':
      break;
    case 'weekly':
      break;
    case 'monthly':
      break;
  }

}

const getDates = (startDate, period, bookingType) => {
  switch(bookingType) {
    case 'weekly':

      break;
    case 'monthly':
      break;
  }
}

export { calcTotal, getDates };
